{
  "name": "cli-file-select",
  "version": "0.0.0",
  "description": "Select a file/folder from the filesystem with cli I/O prompts",
  "main": "dist/index.js",
  "bin": "dist/index.js",
  "typings": "dist/*.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts",
    "watch": "tsup src/index.ts --watch",
    "ts": "tsc",
    "ts:watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "file",
    "folder",
    "selector",
    "picker",
    "cli"
  ],
  "author": "mykeels",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.14.2",
    "@types/prompts": "^2.4.2",
    "tsup": "^6.6.3",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "cac": "^6.7.14",
    "prompts": "^2.4.2"
  }
}
